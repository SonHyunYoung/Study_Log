# 테스트 시나리오 

## 1. 회원가입 및 인증 (Authentication)
사용자 식별 및 보안 정책 준수 여부를 검증합니다.

| ID | 테스트 항목 | 테스트 절차 | 기대 결과 | 실제 결과 | 상태 |
| :--- | :--- | :--- | :--- | :--- | :---: |
| AUTH-01 | **닉네임 길이 제한** | 가입 시 16자의 닉네임 입력 | "이름은 15자까지 입력 가능" 알림 및 차단 | 백엔드 유효성 검사로 차단 확인 | **Pass** |
| AUTH-02 | 이메일 중복 체크 | 이미 가입된 이메일로 가입 시도 | "사용 중인 이메일입니다" 메시지 출력 | 중복 이메일 가입 차단됨 | **Pass** |
| AUTH-03 | 비밀번호 정규식 | 특수문자/대문자 미포함 비번 입력 | 보안 정책 미준수 경고 및 가입 차단 | PASSWORD_REGEX 정상 작동 | **Pass** |
| AUTH-04 | 비인증 접근 차단 | 토큰 없이 `/main` 경로 직접 진입 | 로그인 페이지로 자동 리다이렉트 | Auth 미들웨어 정상 작동 | **Pass** |

---

## 2. 학습 데이터 관리 (Problem CRUD)
데이터베이스 통합(`memo`) 및 예외 처리 로직을 검증합니다.

| ID | 테스트 항목 | 테스트 절차 | 기대 결과 | 실제 결과 | 상태 |
| :--- | :--- | :--- | :--- | :--- | :---: |
| CRUD-01 | 신규 문제 등록 | 문제 번호/언어/성공 여부/메모 입력 | DB `problemtbl`의 `memo` 컬럼에 정상 저장 | 데이터 수신 및 저장 성공 | **Pass** |
| CRUD-02 | 데이터 중복 등록 차단 | 이미 등록된 문제 재등록 시도| 저장 에러가 나면서 등록 불가. | 중복 등록 불가 확인 | **Pass** |
| CRUD-03 | 데이터 조회 (GET) | 메인/목록 페이지 진입 | 통합된 데이터가 화면에 정상 출력 | SQL 쿼리 매핑 정상 확인 | **Pass** |
| CRUD-04 | 데이터 수정 | 상세 모달에서 언어 및 메모 수정 | 수정된 내용이 DB에 반영 및 UI 갱신 | PUT 요청 및 성공 응답 확인 | **Pass** |
| CRUD-05 | 데이터 삭제 | 상세 모달에서 [삭제] 버튼 클릭 | DB에서 삭제되고 목록에서 즉시 제거됨 | PK(id) 기준 삭제 로직 확인 | **Pass** |

---

## 3. 메인 대시보드 (Dashboard)
실시간 통계 데이터의 시각화 및 정확성을 검증합니다.

| ID | 테스트 항목 | 테스트 절차 | 기대 결과 | 실제 결과 | 상태 |
| :--- | :--- | :--- | :--- | :--- | :---: |
| MAIN-01 | 통계 위젯 동기화 | 문제 등록/삭제 후 수치 확인 | 전체/성공/실패 수치가 실시간 동기화됨 | DB Count값과 위젯 수치 일치 | **Pass** |
| MAIN-02 | 난이도 분포 차트 | 티어별 문제 데이터 입력 | 상/중/하 비율이 차트에 정확히 반영됨 | Recharts 시각화 정상 노출 | **Pass** |
| MAIN-03 | 복습 필요 리스트 | 'FAIL' 상태 문제 다수 등록 | 최신순 정렬 및 상위 3개만 위젯 노출 | 데이터 필터링 및 정렬 확인 | **Pass** |

---

## 4. 오답 노트 및 상태 전이 (Incorrect Note)
프로젝트 핵심 로직인 오답 정복 및 데이터 무결성을 검증합니다.

| ID | 테스트 항목 | 테스트 절차 | 기대 결과 | 실제 결과 | 상태 |
| :--- | :--- | :--- | :--- | :--- | :---: |
| INC-01 | 오답 리스트 필터링 | 'FAIL' 상태인 문제만 노출 확인 | 오답 노트 목록에 'FAIL' 데이터만 출력 | SQL WHERE 조건 정상 확인 | **Pass** |
| INC-02 | 복습 완료 | 오답 클릭 → 오답 노트 작성 → 저장 | 상태가 'RETRY_SUCCESS'로 자동 변경 | 데이터 무결성 유지 및 상태 변경 | **Pass** |
| INC-03 | 목록 자동 제거 | 복습 완료 처리 직후 목록 확인 | 해당 문제가 오답 리스트에서 즉시 제거됨 | UI 자동 갱신 및 필터링 확인 | **Pass** |

---

